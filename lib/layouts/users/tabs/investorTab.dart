import 'package:flutter/material.dart';

class InvestorTab extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('Investor Tab'),
      ),
    );
  }
}