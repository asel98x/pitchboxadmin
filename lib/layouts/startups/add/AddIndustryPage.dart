import 'package:flutter/material.dart';

class AddBusinessPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('Add Business Page '),
      ),
    );
  }
}