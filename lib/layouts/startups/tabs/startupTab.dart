import 'package:flutter/material.dart';

class StartupTab extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('startup asd Tab'),
      ),
    );
  }
}